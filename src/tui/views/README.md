# TUI Views

Larger presentational regions live here. The current stream-first shell uses a message stream plus a workspace sidebar; artifact content and SubAgent cards are rendered in the stream, while the sidebar keeps active Agent state and the fixed artifact-root index visible.
