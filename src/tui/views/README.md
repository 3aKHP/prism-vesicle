# TUI Views

Larger presentational regions live here. The current stream-first shell uses a
message stream plus a workspace sidebar; artifact content is rendered in the
stream while the sidebar groups the fixed artifact roots as an index.
